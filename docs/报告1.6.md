# 南锣鼓巷旅游导览系统 - 项目报告

## 一、POI详情模块

### 1.1 模块功能

POI（Point of Interest）详情模块用于展示单个景点的完整信息，是用户深入了解景点的核心页面。

**主要功能：**
- 展示景点名称和类型标签
- 展示景点图片（支持轮播和预览）
- 展示详细文字介绍（不少于100字）
- 展示位置信息
- 展示开放时间和游览提示
- 提供返回列表的导航功能

### 1.2 数据结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  POI 详情数据结构                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  {                                                                          │
│      "id": 1,                          // 景点ID                            │
│      "name": "茅盾故居",                // 景点名称                          │
│      "type": "celebrity",              // 类型代码                          │
│      "type_display": "名人故居",        // 类型显示名                        │
│      "description": "茅盾故居位于...",  // 详细介绍（≥100字）                │
│      "brief": "中国现代著名作家...",    // 简短描述                          │
│      "location": "后圆恩寺胡同13号",    // 具体位置                          │
│      "coordinates": {                  // 经纬度坐标                        │
│          "lat": 39.9375,                                                    │
│          "lng": 116.4039                                                    │
│      },                                                                     │
│      "images": [                       // 图片列表                          │
│          "/images/poi/maodun1.jpg",                                         │
│          "/images/poi/maodun2.jpg"                                          │
│      ],                                                                     │
│      "opening_hours": "9:00-16:30",    // 开放时间                          │
│      "tips": "周一闭馆，需提前预约",    // 游览提示                          │
│      "created_at": "2024-01-01T00:00:00Z"                                   │
│  }                                                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 API 接口

**接口地址：** `GET /api/pois/{id}/`

**请求示例：** `GET /api/pois/1/`

**响应状态码：**
| 状态码 | 说明 |
|--------|------|
| 200 | 成功返回景点详情 |
| 404 | 景点不存在 |


### 1.4 页面流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          POI详情页面流程图                                   │
└─────────────────────────────────────────────────────────────────────────────┘

                        ┌─────────────────┐
                        │ 用户点击景点卡片 │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  路由跳转        │
                        │ /poi/:id        │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  获取路由参数 id │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  调用 API 接口   │
                        │GET /api/pois/id/│
                        └────────┬────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
           ┌───────────────┐         ┌───────────────┐
           │   加载成功     │         │   加载失败     │
           └───────┬───────┘         └───────┬───────┘
                   │                         │
                   ▼                         ▼
           ┌───────────────┐         ┌───────────────┐
           │  渲染详情页面  │         │  显示错误提示  │
           └───────┬───────┘         │  或404页面    │
                   │                 └───────────────┘
     ┌─────────────┼─────────────┬─────────────┐
     │             │             │             │
     ▼             ▼             ▼             ▼
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│图片轮播 │ │基本信息 │ │开放时间 │ │详细介绍 │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
                   │
                   ▼
           ┌───────────────┐
           │   返回按钮     │
           └───────┬───────┘
                   │
                   ▼
           ┌───────────────┐
           │  返回景点列表  │
           └───────────────┘
```

### 1.5 界面设计

#### Web端详情页布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ← 返回列表                                                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ╔═══════════════════════════════════════════════════════════════════════╗  │
│  ║                                                                       ║  │
│  ║                         图片轮播区域                                   ║  │
│  ║                                                                       ║  │
│  ║                    [  ●  ○  ○  ]  指示器                              ║  │
│  ╚═══════════════════════════════════════════════════════════════════════╝  │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│    茅盾故居                                              [名人故居]          │
│                                                                             │
│    📍 后圆恩寺胡同13号                                                      │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  ⏰ 开放时间                                                          │  │
│  │  ─────────────────────────────────────────────────────────────────    │  │
│  │  9:00-16:30                                                           │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  💡 游览提示                                                          │  │
│  │  ─────────────────────────────────────────────────────────────────    │  │
│  │  周一闭馆，免费参观需提前预约                                          │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  📖 详细介绍                                                          │  │
│  │  ─────────────────────────────────────────────────────────────────    │  │
│  │      茅盾故居位于后圆恩寺胡同13号，是中国现代著名作家茅盾（沈雁冰）   │  │
│  │  晚年居住和工作的地方。这是一座典型的北京四合院，占地面积约800平方    │  │
│  │  米。茅盾在这里度过了他生命中最后的岁月，创作了大量文学作品。故居内   │  │
│  │  保存着茅盾生前使用的书房、卧室等，陈列着他的手稿、书信、照片等珍贵   │  │
│  │  文物，是了解茅盾生平和中国现代文学史的重要场所。                      │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


### 1.6 核心代码实现

#### 后端序列化器

```python
class POIDetailSerializer(serializers.ModelSerializer):
    """POI详情序列化器"""
    type_display = serializers.CharField(source='get_type_display', read_only=True)

    class Meta:
        model = POI
        fields = [
            'id', 'name', 'type', 'type_display', 'description', 'brief',
            'location', 'coordinates', 'images', 'opening_hours', 'tips', 'created_at'
        ]
```

#### 后端视图

```python
class POIViewSet(viewsets.ReadOnlyModelViewSet):
    """POI视图集"""
    queryset = POI.objects.all().order_by('order', 'id')

    def get_serializer_class(self):
        if self.action == 'list':
            return POIListSerializer
        return POIDetailSerializer  # 详情使用完整序列化器
```

#### 前端页面（Vue3）

```vue
<template>
  <div class="poi-detail-view">
    <!-- 返回按钮 -->
    <el-button @click="$router.back()">← 返回列表</el-button>
    
    <!-- 图片轮播 -->
    <el-carousel v-if="poi.images && poi.images.length">
      <el-carousel-item v-for="img in poi.images" :key="img">
        <img :src="img" :alt="poi.name" />
      </el-carousel-item>
    </el-carousel>
    
    <!-- 基本信息 -->
    <div class="poi-header">
      <h1>{{ poi.name }}</h1>
      <el-tag>{{ poi.type_display }}</el-tag>
    </div>
    
    <!-- 详细信息卡片 -->
    <div class="info-card" v-if="poi.opening_hours">
      <h3>⏰ 开放时间</h3>
      <p>{{ poi.opening_hours }}</p>
    </div>
    
    <div class="info-card">
      <h3>📖 详细介绍</h3>
      <p>{{ poi.description }}</p>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { useRoute } from 'vue-router'
import { getPOIDetail } from '../api'

const route = useRoute()
const poi = ref({})

onMounted(async () => {
  const { data } = await getPOIDetail(route.params.id)
  poi.value = data
})
</script>
```

---

## 二、路线展示模块

### 2.1 模块功能

路线展示模块用于展示推荐的游览路线，帮助游客规划行程。

**主要功能：**
- 展示路线列表（1-2条推荐路线）
- 展示路线基本信息（名称、时间、距离）
- 展示路线包含的景点（按顺序）
- 支持展开/收起路线详情
- 点击景点可跳转到详情页

### 2.2 数据结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  路线列表数据结构                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  {                                                                          │
│      "count": 2,                                                            │
│      "results": [                                                           │
│          {                                                                  │
│              "id": 1,                                                       │
│              "name": "经典文化游",                                           │
│              "description": "探访名人故居，感受文化底蕴...",                 │
│              "duration": "约2小时",                                         │
│              "distance": "约1.5公里",                                       │
│              "poi_count": 4                                                 │
│          },                                                                 │
│          {                                                                  │
│              "id": 2,                                                       │
│              "name": "休闲美食游",                                           │
│              "description": "品尝特色小吃，逛文创小店...",                   │
│              "duration": "约1.5小时",                                       │
│              "distance": "约1公里",                                         │
│              "poi_count": 4                                                 │
│          }                                                                  │
│      ]                                                                      │
│  }                                                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  路线详情数据结构                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  {                                                                          │
│      "id": 1,                                                               │
│      "name": "经典文化游",                                                   │
│      "description": "探访名人故居，感受深厚的文化底蕴...",                   │
│      "duration": "约2小时",                                                 │
│      "distance": "约1.5公里",                                               │
│      "pois": [                         // 按顺序排列的景点                   │
│          {                                                                  │
│              "order": 1,               // 游览顺序                          │
│              "poi": {                  // 景点信息                          │
│                  "id": 1,                                                   │
│                  "name": "茅盾故居",                                         │
│                  "type": "celebrity",                                       │
│                  "type_display": "名人故居",                                 │
│                  "brief": "中国现代著名作家茅盾晚年居住地"                   │
│              }                                                              │
│          },                                                                 │
│          { "order": 2, "poi": {...} },                                      │
│          { "order": 3, "poi": {...} },                                      │
│          { "order": 4, "poi": {...} }                                       │
│      ]                                                                      │
│  }                                                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```



### 2.3 API 接口

**路线列表接口：** `GET /api/routes/`

| 字段 | 类型 | 说明 |
|------|------|------|
| count | int | 路线总数 |
| results | array | 路线列表 |

**路线详情接口：** `GET /api/routes/{id}/`

| 状态码 | 说明 |
|--------|------|
| 200 | 成功返回路线详情（含景点列表） |
| 404 | 路线不存在 |


### 2.4 页面流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          路线展示模块流程图                                  │
└─────────────────────────────────────────────────────────────────────────────┘

                        ┌─────────────────┐
                        │  进入路线页面    │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  调用路线列表API │
                        │ GET /api/routes/│
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  渲染路线卡片    │
                        │  (折叠状态)      │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  用户点击路线    │
                        │  "查看详情"      │
                        └────────┬────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
           ┌───────────────┐         ┌───────────────┐
           │  已展开状态    │         │  折叠状态      │
           │  (收起路线)    │         │  (展开路线)    │
           └───────┬───────┘         └───────┬───────┘
                   │                         │
                   ▼                         ▼
           ┌───────────────┐         ┌───────────────┐
           │  隐藏景点列表  │         │ 调用详情API   │
           └───────────────┘         │GET /routes/id/│
                                     └───────┬───────┘
                                             │
                                             ▼
                                     ┌───────────────┐
                                     │ 展示时间线    │
                                     │ (景点顺序)    │
                                     └───────┬───────┘
                                             │
                                             ▼
                                     ┌───────────────┐
                                     │ 用户点击景点  │
                                     └───────┬───────┘
                                             │
                                             ▼
                                     ┌───────────────┐
                                     │ 跳转POI详情页 │
                                     └───────────────┘
```


### 2.5 界面设计

#### Web端路线页面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           🗺️ 推荐路线                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ╔═══════════════════════════════════════════════════════════════════════╗  │
│  ║  经典文化游                                                           ║  │
│  ║  ─────────────────────────────────────────────────────────────────    ║  │
│  ║  探访名人故居，感受深厚的文化底蕴，了解南锣鼓巷的历史变迁             ║  │
│  ║                                                                       ║  │
│  ║  ⏱️ 约2小时    📏 约1.5公里    📍 4个景点                             ║  │
│  ║                                                                       ║  │
│  ║                                          [ 查看详情 ▼ ]               ║  │
│  ╚═══════════════════════════════════════════════════════════════════════╝  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  路线景点                                                             │  │
│  │  ─────────────────────────────────────────────────────────────────    │  │
│  │                                                                       │  │
│  │      ●─── 第1站 ───────────────────────────────────────────────       │  │
│  │      │    茅盾故居  [名人故居]                                        │  │
│  │      │    中国现代著名作家茅盾晚年居住地                              │  │
│  │      │                                                                │  │
│  │      ●─── 第2站 ───────────────────────────────────────────────       │  │
│  │      │    齐白石旧居纪念馆  [名人故居]                                │  │
│  │      │    国画大师齐白石曾在此居住创作                                │  │
│  │      │                                                                │  │
│  │      ●─── 第3站 ───────────────────────────────────────────────       │  │
│  │      │    僧格林沁王府  [历史建筑]                                    │  │
│  │      │    清代蒙古亲王僧格林沁的府邸                                  │  │
│  │      │                                                                │  │
│  │      ●─── 第4站 ───────────────────────────────────────────────       │  │
│  │           万庆当铺  [历史建筑]                                        │  │
│  │           清末民初著名当铺，见证商业繁华                              │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ╔═══════════════════════════════════════════════════════════════════════╗  │
│  ║  休闲美食游                                                           ║  │
│  ║  ─────────────────────────────────────────────────────────────────    ║  │
│  ║  品尝特色小吃，逛文创小店，体验老北京的市井生活                       ║  │
│  ║                                                                       ║  │
│  ║  ⏱️ 约1.5小时    📏 约1公里    📍 4个景点                             ║  │
│  ║                                                                       ║  │
│  ║                                          [ 查看详情 ▼ ]               ║  │
│  ╚═══════════════════════════════════════════════════════════════════════╝  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


### 2.6 核心代码实现

#### 后端序列化器

```python
class RouteListSerializer(serializers.ModelSerializer):
    """路线列表序列化器"""
    poi_count = serializers.SerializerMethodField()

    class Meta:
        model = Route
        fields = ['id', 'name', 'description', 'duration', 'distance', 'poi_count']

    def get_poi_count(self, obj):
        return obj.route_pois.count()


class RouteDetailSerializer(serializers.ModelSerializer):
    """路线详情序列化器"""
    pois = serializers.SerializerMethodField()

    class Meta:
        model = Route
        fields = ['id', 'name', 'description', 'duration', 'distance', 'pois']

    def get_pois(self, obj):
        route_pois = obj.route_pois.select_related('poi').order_by('order')
        return [{
            'order': rp.order,
            'poi': POIListSerializer(rp.poi).data
        } for rp in route_pois]
```

#### 后端视图

```python
class RouteViewSet(viewsets.ReadOnlyModelViewSet):
    """推荐路线视图集"""
    queryset = Route.objects.all()

    def get_serializer_class(self):
        if self.action == 'list':
            return RouteListSerializer
        return RouteDetailSerializer

    def list(self, request):
        """获取路线列表"""
        queryset = self.get_queryset()
        serializer = RouteListSerializer(queryset, many=True)
        return Response({
            'count': queryset.count(),
            'results': serializer.data
        })
```

#### 前端页面（Vue3）

```vue
<template>
  <div class="routes-view">
    <h1>🗺️ 推荐路线</h1>
    
    <div v-for="route in routes" :key="route.id" class="route-section">
      <!-- 路线卡片 -->
      <RouteCard :route="route" @view="viewRouteDetail" />
      
      <!-- 展开的路线详情 -->
      <div v-if="selectedRoute === route.id && routeDetail" class="route-detail">
        <h3>路线景点</h3>
        <el-timeline>
          <el-timeline-item
            v-for="item in routeDetail.pois"
            :key="item.order"
            :timestamp="`第${item.order}站`"
          >
            <div class="timeline-poi" @click="$router.push(`/poi/${item.poi.id}`)">
              <strong>{{ item.poi.name }}</strong>
              <el-tag size="small">{{ item.poi.type_display }}</el-tag>
              <p>{{ item.poi.brief }}</p>
            </div>
          </el-timeline-item>
        </el-timeline>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'
import { getRouteList, getRouteDetail } from '../api'

const routes = ref([])
const selectedRoute = ref(null)
const routeDetail = ref(null)

// 切换路线详情展开/收起
const viewRouteDetail = async (id) => {
  if (selectedRoute.value === id) {
    selectedRoute.value = null  // 收起
    routeDetail.value = null
    return
  }
  const { data } = await getRouteDetail(id)
  routeDetail.value = data
  selectedRoute.value = id  // 展开
}

onMounted(async () => {
  const { data } = await getRouteList()
  routes.value = data.results
})
</script>
```


---

## 三、搜索功能

### 3.1 功能概述

搜索功能允许用户通过关键词快速查找感兴趣的景点，支持按名称和描述内容进行模糊匹配。

**主要特性：**
- 支持中文关键词搜索
- 同时匹配景点名称和描述内容
- 实时显示搜索结果数量
- 支持清空搜索恢复全部列表
- 无结果时显示友好提示

### 3.2 搜索数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            搜索功能数据流图                                  │
└─────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────┐                                              ┌─────────────┐
  │   用户输入   │                                              │  搜索结果   │
  │   关键词     │                                              │  展示列表   │
  └──────┬──────┘                                              └──────▲──────┘
         │                                                            │
         │ 1.输入                                              6.渲染 │
         ▼                                                            │
  ┌─────────────┐      2.触发搜索      ┌─────────────┐      5.返回    │
  │  SearchBar  │ ──────────────────▶ │  POIListView │ ◀──────────────┤
  │   组件      │                      │    页面      │                │
  └─────────────┘                      └──────┬──────┘                │
                                              │                        │
                                              │ 3.API请求              │
                                              ▼                        │
                                       ┌─────────────┐                │
                                       │  API模块    │                │
                                       │ searchPOIs  │                │
                                       └──────┬──────┘                │
                                              │                        │
                                              │ HTTP GET               │
                                              ▼                        │
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                              后端服务器                                  │
  │  ┌─────────────────────────────────────────────────────────────────┐    │
  │  │  GET /api/search/?q=关键词                                      │    │
  │  │                                                                 │    │
  │  │  @api_view(['GET'])                                             │    │
  │  │  def search_pois(request):                                      │    │
  │  │      keyword = request.query_params.get('q', '')                │    │
  │  │      pois = POI.objects.filter(                                 │    │
  │  │          Q(name__icontains=keyword) |                           │    │
  │  │          Q(description__icontains=keyword)                      │    │
  │  │      )                                                          │    │
  │  │      return Response({'count': n, 'results': [...]})            │    │
  │  └─────────────────────────────────────────────────────────────────┘    │
  │                                    │                                    │
  │                                    │ 4.查询数据库                        │
  │                                    ▼                                    │
  │                             ┌─────────────┐                             │
  │                             │   SQLite    │                             │
  │                             │   数据库    │                             │
  │                             └─────────────┘                             │
  └─────────────────────────────────────────────────────────────────────────┘
```


### 3.3 API 接口

**接口地址：** `GET /api/search/`

**请求参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| q | string | 是 | 搜索关键词 |

**响应示例：**
```json
{
    "count": 2,
    "results": [
        {
            "id": 1,
            "name": "茅盾故居",
            "type": "celebrity",
            "type_display": "名人故居",
            "brief": "中国现代著名作家茅盾晚年居住地"
        },
        {
            "id": 2,
            "name": "齐白石旧居纪念馆",
            "type": "celebrity",
            "type_display": "名人故居",
            "brief": "国画大师齐白石曾在此居住创作"
        }
    ]
}
```

**搜索逻辑：**
- 使用 Django ORM 的 `Q` 对象实现 OR 查询
- `icontains` 实现大小写不敏感的模糊匹配
- 同时搜索 `name` 和 `description` 字段


### 3.4 界面设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           🏠 景点列表                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                    ┌─────────────────────────────────┐                      │
│                    │  搜索景点...              🔍    │                      │
│                    └─────────────────────────────────┘                      │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  搜索 "故居" 后的结果：                                                     │
│                                                                             │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────────┐  │
│  │  ┌───────────────┐  │  │  ┌───────────────┐  │  │  ┌───────────────┐  │  │
│  │  │               │  │  │  │               │  │  │  │               │  │  │
│  │  │    图片       │  │  │  │    图片       │  │  │  │    图片       │  │  │
│  │  │               │  │  │  │               │  │  │  │               │  │  │
│  │  └───────────────┘  │  │  └───────────────┘  │  │  └───────────────┘  │  │
│  │                     │  │                     │  │                     │  │
│  │  茅盾故居           │  │  齐白石旧居纪念馆   │  │  蒋介石行辕         │  │
│  │  [名人故居]         │  │  [名人故居]         │  │  [名人故居]         │  │
│  │                     │  │                     │  │                     │  │
│  │  中国现代著名作家   │  │  国画大师齐白石曾   │  │  蒋介石在北平期间   │  │
│  │  茅盾晚年居住地     │  │  在此居住创作       │  │  的临时住所         │  │
│  │                     │  │                     │  │                     │  │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────────┘  │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  无搜索结果时：                                                             │
│                                                                             │
│                         ┌─────────────────┐                                 │
│                         │                 │                                 │
│                         │    (空状态)     │                                 │
│                         │                 │                                 │
│                         │ 未找到相关景点  │                                 │
│                         │                 │                                 │
│                         └─────────────────┘                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


### 3.5 核心代码实现

#### 后端搜索视图

```python
from django.db.models import Q

@api_view(['GET'])
def search_pois(request):
    """搜索POI"""
    keyword = request.query_params.get('q', '').strip()
    
    # 空关键词返回提示
    if not keyword:
        return Response({
            'count': 0,
            'results': [],
            'message': '请输入搜索关键词'
        })

    # 使用Q对象实现OR查询，同时匹配名称和描述
    pois = POI.objects.filter(
        Q(name__icontains=keyword) | Q(description__icontains=keyword)
    ).order_by('order', 'id')

    serializer = POIListSerializer(pois, many=True)
    return Response({
        'count': pois.count(),
        'results': serializer.data
    })
```

#### 前端搜索组件

```vue
<!-- SearchBar.vue -->
<template>
  <div class="search-bar">
    <el-input
      v-model="keyword"
      placeholder="搜索景点..."
      clearable
      @keyup.enter="handleSearch"
      @clear="handleClear"
    >
      <template #append>
        <el-button @click="handleSearch">🔍</el-button>
      </template>
    </el-input>
  </div>
</template>

<script setup>
import { ref } from 'vue'

const emit = defineEmits(['search', 'clear'])
const keyword = ref('')

const handleSearch = () => {
  emit('search', keyword.value.trim())
}

const handleClear = () => {
  keyword.value = ''
  emit('clear')
}
</script>
```

#### 前端搜索调用

```vue
<!-- POIListView.vue -->
<script setup>
import { ref, onMounted } from 'vue'
import { getPOIList, searchPOIs } from '../api'

const pois = ref([])
const searchMode = ref(false)

// 加载全部景点
const loadPOIs = async () => {
  searchMode.value = false
  const { data } = await getPOIList()
  pois.value = data.results
}

// 搜索景点
const handleSearch = async (keyword) => {
  if (!keyword) {
    loadPOIs()
    return
  }
  searchMode.value = true
  const { data } = await searchPOIs(keyword)
  pois.value = data.results
}
</script>
```

#### API模块

```javascript
// api/index.js
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://127.0.0.1:8000/api'
})

// 搜索景点
export const searchPOIs = (keyword) => api.get('/search/', { params: { q: keyword } })
```


---

## 四、集成测试报告

### 4.1 测试环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 10/11 |
| 后端环境 | Python 3.x + Django 4.2 + DRF 3.14 |
| 前端环境 | Node.js 16.20.2 + Vue 3 + Vite 4.x |
| 数据库 | SQLite 3 |
| 浏览器 | Chrome 120+ / Edge 120+ |
| 后端地址 | http://127.0.0.1:8000 |
| 前端地址 | http://127.0.0.1:3000 |


### 4.2 测试用例与结果

#### 4.2.1 后端API测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          后端API测试结果                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  测试编号   接口                    预期结果           实际结果    状态     │
│  ─────────────────────────────────────────────────────────────────────────  │
│  API-01    GET /api/hutong/        返回胡同信息        正确返回    ✓ 通过   │
│  API-02    GET /api/pois/          返回POI列表         正确返回    ✓ 通过   │
│  API-03    GET /api/pois/1/        返回POI详情         正确返回    ✓ 通过   │
│  API-04    GET /api/pois/999/      返回404             404错误     ✓ 通过   │
│  API-05    GET /api/routes/        返回路线列表        正确返回    ✓ 通过   │
│  API-06    GET /api/routes/1/      返回路线详情        正确返回    ✓ 通过   │
│  API-07    GET /api/search/?q=故居 返回匹配结果        正确返回    ✓ 通过   │
│  API-08    GET /api/search/?q=     返回空结果提示      正确返回    ✓ 通过   │
│  API-09    GET /api/search/?q=xyz  返回空列表          正确返回    ✓ 通过   │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│  测试通过率: 9/9 = 100%                                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


#### 4.2.2 前端功能测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          前端功能测试结果                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  测试编号   功能模块        测试内容                   实际结果    状态     │
│  ─────────────────────────────────────────────────────────────────────────  │
│  FE-01     首页            胡同信息展示               正确展示    ✓ 通过   │
│  FE-02     首页            快捷入口跳转               正确跳转    ✓ 通过   │
│  FE-03     景点列表        POI卡片展示                正确展示    ✓ 通过   │
│  FE-04     景点列表        点击卡片跳转详情           正确跳转    ✓ 通过   │
│  FE-05     景点详情        图片轮播功能               正常工作    ✓ 通过   │
│  FE-06     景点详情        详细信息展示               正确展示    ✓ 通过   │
│  FE-07     景点详情        返回按钮功能               正确返回    ✓ 通过   │
│  FE-08     路线页面        路线卡片展示               正确展示    ✓ 通过   │
│  FE-09     路线页面        展开/收起路线详情          正常工作    ✓ 通过   │
│  FE-10     路线页面        时间线景点展示             正确展示    ✓ 通过   │
│  FE-11     路线页面        点击景点跳转详情           正确跳转    ✓ 通过   │
│  FE-12     搜索功能        输入关键词搜索             正确搜索    ✓ 通过   │
│  FE-13     搜索功能        回车触发搜索               正常工作    ✓ 通过   │
│  FE-14     搜索功能        清空搜索恢复列表           正常工作    ✓ 通过   │
│  FE-15     搜索功能        无结果提示                 正确提示    ✓ 通过   │
│  FE-16     导航            页面切换                   正常工作    ✓ 通过   │
│  FE-17     响应式          移动端适配                 正常显示    ✓ 通过   │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│  测试通过率: 17/17 = 100%                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 4.2.3 小程序功能测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          小程序功能测试结果                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  测试编号   功能模块        测试内容                   实际结果    状态     │
│  ─────────────────────────────────────────────────────────────────────────  │
│  MP-01     首页            胡同信息展示               正确展示    ✓ 通过   │
│  MP-02     首页            功能入口跳转               正确跳转    ✓ 通过   │
│  MP-03     景点列表        搜索功能                   正常工作    ✓ 通过   │
│  MP-04     景点列表        POI卡片展示                正确展示    ✓ 通过   │
│  MP-05     景点详情        图片预览功能               正常工作    ✓ 通过   │
│  MP-06     景点详情        详细信息展示               正确展示    ✓ 通过   │
│  MP-07     路线页面        路线列表展示               正确展示    ✓ 通过   │
│  MP-08     路线页面        展开/收起功能              正常工作    ✓ 通过   │
│  MP-09     TabBar          底部导航切换               正常工作    ✓ 通过   │
│  MP-10     网络请求        API数据获取                正确获取    ✓ 通过   │
│                                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│  测试通过率: 10/10 = 100%                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```


### 4.3 集成测试流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          集成测试流程图                                      │
└─────────────────────────────────────────────────────────────────────────────┘

                        ┌─────────────────┐
                        │   启动后端服务   │
                        │ python manage.py│
                        │   runserver     │
                        └────────┬────────┘
                                 │
                                 ▼
                        ┌─────────────────┐
                        │  验证后端API    │
                        │  访问 :8000/api │
                        └────────┬────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
           ┌───────────────┐         ┌───────────────┐
           │   API正常      │         │   API异常      │
           └───────┬───────┘         └───────┬───────┘
                   │                         │
                   │                         ▼
                   │                 ┌───────────────┐
                   │                 │  检查数据库    │
                   │                 │  检查配置      │
                   │                 └───────────────┘
                   │
                   ▼
           ┌───────────────┐
           │  启动前端服务  │
           │  npm run dev  │
           └───────┬───────┘
                   │
                   ▼
           ┌───────────────┐
           │  访问前端页面  │
           │  :3000        │
           └───────┬───────┘
                   │
     ┌─────────────┼─────────────┬─────────────┐
     │             │             │             │
     ▼             ▼             ▼             ▼
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│测试首页 │ │测试景点 │ │测试路线 │ │测试搜索 │
└────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘
     │           │           │           │
     └─────────────────┬─────────────────┘
                       │
                       ▼
               ┌───────────────┐
               │  记录测试结果  │
               └───────┬───────┘
                       │
          ┌────────────┴────────────┐
          │                         │
          ▼                         ▼
   ┌─────────────┐          ┌─────────────┐
   │  全部通过    │          │  存在问题    │
   │  测试完成    │          │  修复后重测  │
   └─────────────┘          └─────────────┘
```


### 4.4 测试总结

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            测试总结报告                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  测试统计                                                           │    │
│  │  ───────────────────────────────────────────────────────────────    │    │
│  │                                                                     │    │
│  │    测试类型          用例数      通过数      通过率                 │    │
│  │    ─────────────────────────────────────────────────────────────    │    │
│  │    后端API测试         9           9         100%                   │    │
│  │    前端功能测试       17          17         100%                   │    │
│  │    小程序功能测试     10          10         100%                   │    │
│  │    ─────────────────────────────────────────────────────────────    │    │
│  │    总计               36          36         100%                   │    │
│  │                                                                     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  测试结论                                                           │    │
│  │  ───────────────────────────────────────────────────────────────    │    │
│  │                                                                     │    │
│  │  1. 所有API接口均能正常响应，数据格式符合预期                       │    │
│  │  2. 前端页面功能完整，用户交互流畅                                  │    │
│  │  3. 前后端数据交互正常，CORS配置正确                                │    │
│  │  4. 搜索功能支持中文，模糊匹配准确                                  │    │
│  │  5. 路线展开/收起功能正常，景点顺序正确                             │    │
│  │  6. 小程序功能与Web端保持一致                                       │    │
│  │  7. 系统整体运行稳定，满足设计要求                                  │    │
│  │                                                                     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 附录：启动命令

### 后端启动
```bash
cd backend
.\venv\Scripts\python.exe manage.py runserver
```

### 前端启动
```bash
cd frontend
npm run dev
```

### 访问地址
- 后端API: http://127.0.0.1:8000/api/
- 后端管理: http://127.0.0.1:8000/admin/
- 前端页面: http://127.0.0.1:3000/



## 项目成员：郭琼鹏 汪凯艳
